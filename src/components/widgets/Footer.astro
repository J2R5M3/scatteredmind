---
import { footerData } from '~/navigation';
import { Icon } from 'astro-icon/components';
---

<footer class="bg-black/20 border-t-4 border-card-border">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-8">
    <div class="flex flex-col items-center justify-between gap-4 md:flex-row">
      <div class="flex gap-6">
        {
          footerData.socialLinks.map((link) => (
            <a class="text-gray-500 hover:text-primary transition-colors" href={link.href} aria-label={link.ariaLabel}>
              <Icon name={link.icon} class="w-8 h-8" />
            </a>
          ))
        }
      </div>
      <p class="text-xl text-gray-500">© 2024 Scattered Minds. All rights reserved.</p>
    </div>
  </div>
</footer>
